// Nothing to test here...   ...This tdd is for javascript
// For JSX stuff I will use a different ( outside ) mechanism for tdd
// 
const { verdict } = require('./common.js');

if (!module.parent) {
}
